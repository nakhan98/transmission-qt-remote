name: CI

on:
  push:
    branches: [ '*' ]
  pull_request:
    branches: [ '*' ]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.12'

      - name: Install system dependencies
        run: sudo apt-get update && sudo apt-get install -y make

      - name: Install uv
        run: pip install uv

      - name: Set up virtual environment
        run: make setup_venv

      - name: Install dependencies
        run: make install

      - name: Run linting
        run: make lint

  typecheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.12'

      - name: Install system dependencies
        run: sudo apt-get update && sudo apt-get install -y make

      - name: Install uv
        run: pip install uv

      - name: Set up virtual environment
        run: make setup_venv

      - name: Install dependencies
        run: |
          make install
          .venv.Linux_x86_64/bin/pip install pyright PySide6-stubs

      - name: Run type checking
        run: make typecheck

  unit-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.12'

      - name: Install system dependencies
        run: |
          sudo apt-get update && sudo apt-get install -y make \
            libegl1 \
            libgl1 \
            libglib2.0-0 \
            libxcb1 \
            libxkbcommon0 \
            libfontconfig1 \
            libfreetype6

      - name: Install uv
        run: pip install uv

      - name: Set up virtual environment
        run: make setup_venv

      - name: Install dependencies
        run: |
          make install
          .venv.Linux_x86_64/bin/pip install -r requirements.unit.txt

      - name: Run Unit Tests
        run: make test_unit

  integration-tests:
    runs-on: ubuntu-latest
    needs: [lint, typecheck, unit-tests]
    if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/fix/ci-workflow'
    steps:
      - uses: actions/checkout@v4

      - name: Run Integration Tests in Docker
        run: make docker_test_integration